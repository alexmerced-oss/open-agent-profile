# INVALID: violates SPEC 7.3. mcp_servers[].env values must be same-name
# ${VARIABLE} references. A literal credential must be rejected at resolve time.
oap: "1.0"
kind: AgentProfile
metadata:
  name: leaky
  description: Declares an MCP server with a literal token in the environment block.
spec:
  role:
    instructions: You do things.
  tools:
    mcp_servers:
      - name: github
        transport: stdio
        command: github-mcp
        env:
          GITHUB_TOKEN: "ghp_examplenotarealtokenvalue0000000000"
